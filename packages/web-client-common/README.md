# Dominion Web client common

This package contains classes and specifications that are common to both the front-end Web client and the back-end scaffolding for it. It specifies the format of WebSocket messages that will be passed between the two.
