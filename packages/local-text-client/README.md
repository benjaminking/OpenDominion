# Dominion text client

The Dominion text-based client is a lightweight client that allows a human user to play a game of Dominion via a text-based console. It is useful for bypassing the layers of the Web interface and even the Web server itself.
