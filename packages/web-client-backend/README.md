# Dominion Web client backend

This is the server-side back-end for the Dominion Web client. This package implements the `Client` functionality and translates both game engine messages and decision requests into WebSocket messages. It listens for responses and passes them back to the game engine.
